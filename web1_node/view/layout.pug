//-layout template 
html
    head
        title WEB1 - #{title}
        meta(charset='utf-8')
    body
        h1
            a(href='/') WEB
        ul
            for topic in topics
                li
                    a(href='/?id=' + topic.id) #{topic.title}
        block content